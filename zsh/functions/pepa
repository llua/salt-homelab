#autoload

if ! (( $+commands[pepa] )); then
  python2 /usr/(local/)#lib/python2.7/site-packages/salt/pillar/pepa.py -c =(cat /usr/local/etc/salt/master.d/{f,site}_defaults.conf) "$@"
else
  command pepa "$@"
fi
