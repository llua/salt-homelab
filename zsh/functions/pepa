#autoload

if ! (( $+commands[pepa] )); then
  ${${commands[(I)python3.<->]}[1]} /usr/(local/)#lib/${${commands[(I)python3.<->]}[1]}/site-packages/salt/pillar/pepa.py -c /usr/local/etc/salt/master.d/f_defaults.conf "$@"
else
  command pepa "$@"
fi
